export default {
    plugins: {
        '@tailwindcss/postcss': {}, // Use the new PostCSS plugin package
        autoprefixer: {}, // This adds vendor prefixes for broader browser compatibility
    },
};
